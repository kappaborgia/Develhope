spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: test
    password:
  jpa:

    hibernate:
      ddl-auto: create

  h2:
    console:
      enable: true

---
environment: test
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: test
    passeord: test
    driver-class-name: org.h2.Driver
  jpa:
    database: h2
  h2:
    console:
      enabled : true
